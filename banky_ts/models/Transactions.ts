// import mongoose from "mongoose";

// const transactionSchema = new mongoose.Schema({
//   type: { type: String, required: true }, // 'add' | 'withdraw' | 'transfer'
//   amount: { type: Number, required: true },
//   to: { type: String },   // for transfers
//   from: { type: String }, // for incoming transfers
//   date: { type: Date, default: Date.now },
// }, { timestamps: true })

//     export default mongoose.models.Transactions || mongoose.model("Transactions", transactionSchema)